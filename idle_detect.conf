initial_sleep=0
use_event_detect=1
use_xprintidle=1
inactivity_time_trigger="5"
active_command="log \*\*\* active \*\*\*" # replace this with the dc_pause once that is customized to your desire
idle_command="log \*\*\* idle \*\*\*" # replace this with the dc_unpause once that is customized to your desire
debug=1
